<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.D2lesson68HomeWork"
        tools:targetApi="31" >
        <activity
            android:name=".fragments.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".fragments.SponsorActivity"
            android:exported="false" />
        <activity
            android:name=".fragments.DiscriptionActivity"
            android:exported="false" />
        <activity
            android:name=".fragments.CallCenterActivity"
            android:exported="false"
            android:label="@string/title_activity_call_center"
            android:theme="@style/Theme.D2lesson68HomeWork.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>